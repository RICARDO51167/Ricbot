# scripts

构建、冒烟评测、基线和发布门禁脚本。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `eval-baseline.sh`：eval-baseline.sh：可执行的构建、验证或启动脚本。
- `release-check.sh`：release-check.sh：可执行的构建、验证或启动脚本。
- `smoke.sh`：smoke.sh：可执行的构建、验证或启动脚本。

