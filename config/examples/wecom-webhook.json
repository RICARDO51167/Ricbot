{
  "api": {
    "host": "127.0.0.1",
    "port": 8000,
    "bearer_token": "${RICBOT_API_BEARER_TOKEN}"
  },
  "channels": {
    "wecom": {
      "enabled": true,
      "bot_id": "${WECOM_BOT_ID}",
      "secret": "${WECOM_SECRET}",
      "token": "${WECOM_WEBHOOK_TOKEN}",
      "allow_from": ["*"]
    }
  }
}
