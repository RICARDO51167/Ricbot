# config

可提交的运行配置与安全示例；真实密钥必须通过环境变量注入。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `examples/`：可选能力的最小配置样例，用于复制和验证配置结构。

## 直接文件

- `ricbot.config.json`：ricbot.config.json：结构化配置或评测输入。

