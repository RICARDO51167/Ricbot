# wrapper

固定 Maven Wrapper 版本、下载地址与启动所需组件。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `maven-wrapper.jar`：maven-wrapper.jar：Maven Wrapper 启动组件，不是 Ricbot 业务代码。
- `maven-wrapper.properties`：maven-wrapper.properties：运行或构建属性。

