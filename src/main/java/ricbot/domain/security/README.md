# security

副作用审批、幂等预留、补偿和风险证据。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `ApprovalApplicationService.java`：Approval Application Service：封装该领域用例或业务规则。
- `ApprovalDecision.java`：Approval Decision：本包内的领域类型或协作组件。
- `ApprovalRequest.java`：Approval Request：本包内的领域类型或协作组件。
- `ApprovalService.java`：Approval Service：封装该领域用例或业务规则。
- `CommandRiskAnalyzer.java`：Command Risk Analyzer：本包内的领域类型或协作组件。
- `CommandRiskLevel.java`：Command Risk Level：本包内的领域类型或协作组件。
- `PendingToolCall.java`：Pending Tool Call：工具协议、实现或注册逻辑。
- `RiskAssessment.java`：Risk Assessment：本包内的领域类型或协作组件。

