package ricbot.domain.policy;

/** Execution-policy persona metadata; it is not a scheduler or task lifecycle. */
public enum PolicyRole {
    EXPLORER,
    VERIFIER,
    TESTER,
    DEVELOPER,
    LEADER,
    REVIEWER,
    SYNTHESIZER,
    PLANNER
}
