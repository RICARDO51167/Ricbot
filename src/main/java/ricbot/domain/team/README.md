# team

Team 任务、计划步骤、验证、产物和 Worker 协作领域规则。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `ApprovalEvidence.java`：Approval Evidence：本包内的领域类型或协作组件。
- `DiffEvidence.java`：Diff Evidence：本包内的领域类型或协作组件。
- `ExecutedTestEvidence.java`：Executed Test Evidence：本包内的领域类型或协作组件。
- `ImplementationPlanService.java`：Implementation Plan Service：封装该领域用例或业务规则。
- `ImplementationStepGate.java`：Implementation Step Gate：本包内的领域类型或协作组件。
- `ImplementationStepStatus.java`：Implementation Step Status：执行结果、报告或状态值对象。
- `ImplementationStepType.java`：Implementation Step Type：本包内的领域类型或协作组件。
- `PendingImplementationStep.java`：Pending Implementation Step：本包内的领域类型或协作组件。
- `StepAuditCompactor.java`：Step Audit Compactor：本包内的领域类型或协作组件。
- `StepAuditEventType.java`：Step Audit Event Type：事件、消息或审计事实模型。
- `StepAuditHealth.java`：Step Audit Health：本包内的领域类型或协作组件。
- `StepAuditLinker.java`：Step Audit Linker：本包内的领域类型或协作组件。
- `StepAuditRecord.java`：Step Audit Record：本包内的领域类型或协作组件。
- `StepAuditService.java`：Step Audit Service：封装该领域用例或业务规则。
- `StepAuditSummary.java`：Step Audit Summary：执行结果、报告或状态值对象。
- `StepGateResult.java`：Step Gate Result：执行结果、报告或状态值对象。
- `StepUpdateRequest.java`：Step Update Request：本包内的领域类型或协作组件。
- `TeamArtifact.java`：Team Artifact：本包内的领域类型或协作组件。
- `TeamDecisionPolicy.java`：Team Decision Policy：本包内的领域类型或协作组件。
- `TeamEngine.java`：Team Engine：驱动对应执行生命周期。
- `TeamEvent.java`：Team Event：事件、消息或审计事实模型。
- `TeamExecutionService.java`：Team Execution Service：封装该领域用例或业务规则。
- `TeamRole.java`：Team Role：本包内的领域类型或协作组件。
- `TeamSession.java`：Team Session：本包内的领域类型或协作组件。
- `TeamSessionStore.java`：Team Session Store：负责状态持久化、读取或查询。
- `TeamTask.java`：Team Task：本包内的领域类型或协作组件。
- `TeamTaskHealth.java`：Team Task Health：本包内的领域类型或协作组件。
- `TeamTaskReport.java`：Team Task Report：执行结果、报告或状态值对象。
- `TeamTaskReportService.java`：Team Task Report Service：封装该领域用例或业务规则。
- `TeamTaskState.java`：Team Task State：执行结果、报告或状态值对象。
- `TeamTaskStatus.java`：Team Task Status：执行结果、报告或状态值对象。
- `TeamWhiteboard.java`：Team Whiteboard：本包内的领域类型或协作组件。
- `TeamWorkerExecutor.java`：Team Worker Executor：本包内的领域类型或协作组件。
- `TeamWorkerResult.java`：Team Worker Result：执行结果、报告或状态值对象。
- `TeamWorkerRunner.java`：Team Worker Runner：驱动对应执行生命周期。
- `TeamWorkerStatus.java`：Team Worker Status：执行结果、报告或状态值对象。
- `VerificationEvidence.java`：Verification Evidence：本包内的领域类型或协作组件。
- `VerificationInput.java`：Verification Input：本包内的领域类型或协作组件。
- `VerificationResult.java`：Verification Result：执行结果、报告或状态值对象。
- `VerificationService.java`：Verification Service：封装该领域用例或业务规则。
- `VerifierOutputSanitizer.java`：Verifier Output Sanitizer：本包内的领域类型或协作组件。
- `WorkerExecutionInput.java`：Worker Execution Input：本包内的领域类型或协作组件。
- `WorkerExecutionResult.java`：Worker Execution Result：执行结果、报告或状态值对象。

