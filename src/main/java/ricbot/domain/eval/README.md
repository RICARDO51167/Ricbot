# eval

确定性评测、矩阵、回放、对比、快照和报告模型。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `EvalCaseResult.java`：Eval Case Result：执行结果、报告或状态值对象。
- `EvalCaseSummary.java`：Eval Case Summary：执行结果、报告或状态值对象。
- `EvalCompareRunner.java`：Eval Compare Runner：驱动对应执行生命周期。
- `EvalComparisonResult.java`：Eval Comparison Result：执行结果、报告或状态值对象。
- `EvalFailureClassifier.java`：Eval Failure Classifier：本包内的领域类型或协作组件。
- `EvalHarness.java`：Eval Harness：本包内的领域类型或协作组件。
- `EvalLintResult.java`：Eval Lint Result：执行结果、报告或状态值对象。
- `EvalMatrixRunner.java`：Eval Matrix Runner：驱动对应执行生命周期。
- `EvalMatrixSpec.java`：Eval Matrix Spec：声明配置、选项或不可变执行规格。
- `EvalModelTarget.java`：Eval Model Target：本包内的领域类型或协作组件。
- `EvalOptions.java`：Eval Options：声明配置、选项或不可变执行规格。
- `EvalRecordingProvider.java`：Eval Recording Provider：模型 Provider 的出站调用或能力描述。
- `EvalReplayProvider.java`：Eval Replay Provider：模型 Provider 的出站调用或能力描述。
- `EvalReplayRunner.java`：Eval Replay Runner：驱动对应执行生命周期。
- `EvalReportWriter.java`：Eval Report Writer：执行结果、报告或状态值对象。
- `EvalRunSummary.java`：Eval Run Summary：执行结果、报告或状态值对象。
- `EvalRuntimeState.java`：Eval Runtime State：驱动对应执行生命周期。
- `EvalScenario.java`：Eval Scenario：本包内的领域类型或协作组件。
- `EvalScenarioLinter.java`：Eval Scenario Linter：本包内的领域类型或协作组件。
- `EvalSmokeProvider.java`：Eval Smoke Provider：模型 Provider 的出站调用或能力描述。
- `EvalSmokeRuntime.java`：Eval Smoke Runtime：驱动对应执行生命周期。
- `EvalSummarySupport.java`：Eval Summary Support：执行结果、报告或状态值对象。
- `EvalTurn.java`：Eval Turn：本包内的领域类型或协作组件。
- `WorkspaceDiff.java`：Workspace Diff：本包内的领域类型或协作组件。
- `WorkspaceRestorePoint.java`：Workspace Restore Point：本包内的领域类型或协作组件。
- `WorkspaceSnapshot.java`：Workspace Snapshot：本包内的领域类型或协作组件。

