# app

进程入口与依赖装配层；不承载核心业务规则。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `bootstrap/`：创建 Provider、存储、AgentLoop、工具和应用服务的组合根。
- `cli/`：唯一对外入口，负责参数解析、交互渲染、配置向导和命令分发。

## 直接文件

除本说明外无直接文件；实现位于上列子目录。

