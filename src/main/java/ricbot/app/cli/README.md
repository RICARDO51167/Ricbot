# cli

唯一对外入口，负责参数解析、交互渲染、配置向导和命令分发。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `CliCommands.java`：Cli Commands：本包内的领域类型或协作组件。
- `CliModelHelpers.java`：Cli Model Helpers：本包内的领域类型或协作组件。
- `OnboardWizard.java`：Onboard Wizard：本包内的领域类型或协作组件。
- `StreamRenderer.java`：Stream Renderer：本包内的领域类型或协作组件。

