# team

Team/Worker 命令用例，包括会话、任务、步骤、报告、执行和工具审批。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `TeamReportApplicationService.java`：Team Report Application Service：封装该领域用例或业务规则。
- `TeamRunApplicationService.java`：Team Run Application Service：封装该领域用例或业务规则。
- `TeamSessionApplicationService.java`：Team Session Application Service：封装该领域用例或业务规则。
- `TeamStepApplicationService.java`：Team Step Application Service：封装该领域用例或业务规则。
- `TeamTaskApplicationService.java`：Team Task Application Service：封装该领域用例或业务规则。
- `TeamToolApplicationService.java`：Team Tool Application Service：封装该领域用例或业务规则。
- `TeamWorkerApplicationService.java`：Team Worker Application Service：封装该领域用例或业务规则。

