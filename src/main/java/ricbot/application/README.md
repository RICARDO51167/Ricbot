# application

应用服务层，把 CLI 用例编排到领域对象，避免入口直接操纵存储。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `team/`：Team/Worker 命令用例，包括会话、任务、步骤、报告、执行和工具审批。
- `workspace/`：工作区用例编排，连接 Team 任务与隔离工作区生命周期。

## 直接文件

除本说明外无直接文件；实现位于上列子目录。

