# integration

外部系统适配层；当前只包含 CLI 命令适配与模型 Provider。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `command/`：命令处理协议适配，不提供 HTTP/WebSocket 服务。
- `llm/`：大模型 Provider 集成，所有网络访问均为 CLI 发起的出站调用。

## 直接文件

除本说明外无直接文件；实现位于上列子目录。

