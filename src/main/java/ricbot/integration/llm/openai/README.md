# openai

OpenAI-compatible 出站 Provider，可通过 Base URL 连接兼容服务。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `OpenAICompatProvider.java`：Open AICompat Provider：模型 Provider 的出站调用或能力描述。

