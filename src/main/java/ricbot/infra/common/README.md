# common

通用 JSON、哈希、路径和轻量基础组件。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `CircuitBreaker.java`：Circuit Breaker：本包内的领域类型或协作组件。
- `HelperUtils.java`：Helper Utils：本包内的领域类型或协作组件。
- `JsonMapUtils.java`：Json Map Utils：本包内的领域类型或协作组件。
- `RetryUtils.java`：Retry Utils：本包内的领域类型或协作组件。
- `TextParsingUtils.java`：Text Parsing Utils：本包内的领域类型或协作组件。

