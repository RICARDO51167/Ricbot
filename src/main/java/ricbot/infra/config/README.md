# config

配置文件读取、环境变量替换、旧配置拒绝和运行路径解析。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `Config.java`：Config：声明配置、选项或不可变执行规格。
- `ConfigLoader.java`：Config Loader：声明配置、选项或不可变执行规格。
- `RuntimePaths.java`：Runtime Paths：驱动对应执行生命周期。

