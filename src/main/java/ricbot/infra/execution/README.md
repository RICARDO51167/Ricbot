# execution

Local/Docker 执行后端与隔离策略；Docker 默认断网且禁止静默回退。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

无直接子目录。

## 直接文件

- `DockerExecutionBackend.java`：Docker Execution Backend：本包内的领域类型或协作组件。
- `ExecutionBackend.java`：Execution Backend：本包内的领域类型或协作组件。
- `ExecutionBackendFactory.java`：Execution Backend Factory：本包内的领域类型或协作组件。
- `ExecutionBackendRegistry.java`：Execution Backend Registry：本包内的领域类型或协作组件。
- `ExecutionCapabilities.java`：Execution Capabilities：本包内的领域类型或协作组件。
- `ExecutionRequest.java`：Execution Request：本包内的领域类型或协作组件。
- `ExecutionResult.java`：Execution Result：执行结果、报告或状态值对象。
- `LocalExecutionBackend.java`：Local Execution Backend：本包内的领域类型或协作组件。

