# templates

Agent 与 Memory 使用的提示模板根目录。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `agent/`：Agent 身份、Worker 协作和安全片段模板。
- `memory/`：Memory 上下文模板。

## 直接文件

- `SOUL.md`：SOUL.md：人工维护的说明或提示模板。
- `USER.md`：USER.md：人工维护的说明或提示模板。

