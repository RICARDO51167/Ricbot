# domain

核心领域测试集合。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `agent/`：对应生产包 `agent` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `change/`：对应生产包 `change` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `config/`：对应生产包 `config` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `eval/`：对应生产包 `eval` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `memory/`：对应生产包 `memory` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `message/`：对应生产包 `message` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `policy/`：对应生产包 `policy` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `security/`：对应生产包 `security` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `session/`：对应生产包 `session` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `team/`：对应生产包 `team` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `trace/`：对应生产包 `trace` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `worker/`：对应生产包 `worker` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `workspace/`：对应生产包 `workspace` 的自动化测试，验证正常路径、失败边界和持久化不变量。

## 直接文件

除本说明外无直接文件；实现位于上列子目录。

