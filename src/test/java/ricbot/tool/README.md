# tool

工具注册、文件和进程工具测试集合。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `api/`：对应生产包 `api` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `filesystem/`：对应生产包 `filesystem` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `process/`：对应生产包 `process` 的自动化测试，验证正常路径、失败边界和持久化不变量。

## 直接文件

除本说明外无直接文件；实现位于上列子目录。

