# llm

对应生产包 `llm` 的自动化测试，验证正常路径、失败边界和持久化不变量。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `anthropic/`：对应生产包 `anthropic` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `api/`：对应生产包 `api` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `openai/`：对应生产包 `openai` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `provider/`：对应生产包 `provider` 的自动化测试，验证正常路径、失败边界和持久化不变量。

## 直接文件

除本说明外无直接文件；实现位于上列子目录。

