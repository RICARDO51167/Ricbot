# infra

基础设施实现测试集合。

## 边界

本说明只描述当前保留的能力。生成产物、运行数据和已删除模块不属于本目录契约。

## 子目录

- `common/`：对应生产包 `common` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `config/`：对应生产包 `config` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `execution/`：对应生产包 `execution` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `persistence/`：对应生产包 `persistence` 的自动化测试，验证正常路径、失败边界和持久化不变量。
- `telemetry/`：对应生产包 `telemetry` 的自动化测试，验证正常路径、失败边界和持久化不变量。

## 直接文件

除本说明外无直接文件；实现位于上列子目录。

